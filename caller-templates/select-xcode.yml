# Reusable Xcode selection step from the shared github repo.
# Use runs-on: xcode-27 (or xcode-27-xlarge) in the containing job.
- name: Select Xcode
  uses: VeamStudios/.github/.github/actions/select-xcode@main
  with:
    xcode-version: "27.0"
    minimum-swift-version: "6.3"
    # print-version: "true"
