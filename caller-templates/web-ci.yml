# Web CI - Caller Workflow
#
# Copy this file into your web repo's .github/workflows/ directory.
# Runs format check, lint, typecheck, and build on every pull request.

name: CI

on: [pull_request]

permissions:
  contents: read
  packages: read

jobs:
  validate:
    uses: VeamStudios/.github/.github/workflows/web-ci.yml@main
    # with:
    #   node_version: "20.x"
    #   build_command: "build"
