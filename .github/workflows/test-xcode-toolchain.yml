name: Test Xcode Toolchain

on:
  pull_request:
    paths:
      - ".github/actions/select-xcode/**"
      - ".github/workflows/test-xcode-toolchain.yml"
      - ".github/workflows/pr-ios-build.yml"
      - ".github/workflows/ios-tests.yml"
      - ".github/workflows/deploy-ios-testflight.yml"
      - ".github/workflows/hotfix-deploy.yml"
      - ".github/workflows/pr-spm-package-update.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  toolchain:
    name: Xcode 27 (${{ matrix.runner }})
    strategy:
      fail-fast: false
      matrix:
        runner: [xcode-27, xcode-27-xlarge]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Select stable Xcode 27
        uses: ./.github/actions/select-xcode
        with:
          xcode-version: "27.0"
          minimum-swift-version: "6.3"
      - name: Verify iOS SDK and simulator devices
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -showsdks
          xcrun --sdk iphoneos --show-sdk-version | grep -E '^27\.0$'
          xcrun simctl list devices available --json > "$RUNNER_TEMP/simulators.json"
          python3 - "$RUNNER_TEMP/simulators.json" <<'PY'
          import json
          import sys

          with open(sys.argv[1]) as source:
              devices = json.load(source)["devices"]
          available = {
              device["name"]
              for runtime, entries in devices.items()
              if runtime.endswith(".iOS-27-0")
              for device in entries
              if device.get("isAvailable")
          }
          required = {"iPhone 17", "iPhone 18 Pro Max", "iPad Pro 13-inch (M5)"}
          missing = required - available
          if missing:
              raise SystemExit(f"Missing iOS 27 simulators: {sorted(missing)}")
          print(f"Verified iOS 27 simulators: {sorted(required)}")
          PY
