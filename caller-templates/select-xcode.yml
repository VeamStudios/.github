# Reusable Xcode selection step from the shared github repo.
- name: Select Xcode
  uses: VeamStudios/.github/.github/actions/select-xcode@main
  # with:
  #   xcode-version: "26.3"
  #   print-version: "true"
